{
  "name": "allsight",
  "version": "0.1.0",
  "description": "cross-platform prediction market aggregator with execution routing",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "ingestion",
    "normalization",
    "analysis",
    "execution",
    "api",
    "dashboard"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
